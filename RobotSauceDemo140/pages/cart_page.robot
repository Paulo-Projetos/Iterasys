# Neste caso não teremos a seção "Setting" nem a "Keword", pois não exestira ações de execução, más ações somente de leitura

*** Variables ***
${cart_product_name}         css=div.inventory_item_name
${cart_roduct_price}        css=div.inventory_item_price
${cart_product_quantity}     css=div.cart_quantity

